// Import models
import Weather from "./weather.js";
import News from "./news.js";

// Export models
export { Weather, News };

// Export default for convenience
export default {
  Weather,
  News 
};
