{
  "name": "am-coding-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "worker:build": "npx esbuild worker.js --bundle --format=esm --outfile=dist/worker.js",
    "worker:dev": "wrangler dev --test-scheduled",
    "worker:deploy": "wrangler deploy"
  },
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "4.21.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.4",
    "nodemon": "^3.1.10",
    "wrangler": "^4.15.2"
  }
}
